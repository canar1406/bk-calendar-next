{
	"name": "@bkalendar-next/extension",
	"version": "0.5.6",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build && vite build --config vite.config.content.ts && vite build --config vite.config.web-review.ts",
		"check": "tsc --noEmit",
		"test": "node --experimental-strip-types --test test/**/*.test.ts"
	},
	"dependencies": {
		"@bkalendar-next/core": "workspace:*",
		"@bkalendar-next/timetable": "workspace:*"
	},
	"devDependencies": {
		"@types/chrome": "latest",
		"typescript": "~6.0.0",
		"vite": "latest"
	}
}
